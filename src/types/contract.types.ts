export interface Contract {
  buy_price: number;
  contract_id: string;
  contract_type: string;
  currency: string;
  date_start: string;
  expiry_time: string;
  payout: number;
  purchase_time: string;
  symbol: string;
}
