export interface Contract {
  buy_price: number;
  contract_id: string;
  contract_type: string;
  currency: string;
  date_start: string;
  expiry_time: string;
  payout: number;
  purchase_time: string;
  strategy_name: string;
  leader: string;
  leader_id: string;
  strategy_id: string;
}
